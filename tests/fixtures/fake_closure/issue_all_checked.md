Tracks the audit object. All non-waivable requirements satisfied:

- [x] `energy_like_drift` — implemented + tested
- [x] `phase_spread_bound` — implemented + tested
- [x] `solver_metadata` — recorded

**Non-waivable:** the above must all land before closure. They have.
