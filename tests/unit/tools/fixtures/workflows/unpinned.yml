name: unpinned-example
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout latest
        uses: actions/checkout@v5
      - name: Setup Python
        uses: actions/setup-python@v5
