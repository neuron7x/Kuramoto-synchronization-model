[pytest]
# Keep stdlib-only CI helper tests independent from repository-level tests/conftest.py,
# which bootstraps the full scientific stack (pandas, YAML, audit fixtures).
# These validator/helper tests import their targets by path and require no shared fixtures.
