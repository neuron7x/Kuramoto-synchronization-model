# Inference Legend Exit Work Act

Status: canonical work act  
Repository: neuron7xLab/GeoSync  
Scope: Ricci-Kuramoto manifold kernel, epistemic-integrity governance, predictive research validation  
Mode: laboratory-academic, engineering-technical, practice-bound  
Target rank: top-quality reproducible research-engineering artifact  

## 1. Purpose

This act defines the minimum work system required to move GeoSync from an ambitious verification-first research platform into a top-tier reproducible quantitative research artifact.

The target is not rhetorical greatness. The target is operational rank: a system whose claims survive replay, null models, baseline competition, cost models, external reproduction, and claim-tier governance.

The central task is to transform inference from language output into evidence-bearing computation.

## 2. Core thesis

GeoSync becomes high-rank only if every predictive claim is bound to measured artifacts.

A valid inference path must satisfy:

```text
contextualize -> generate -> resist -> compute -> test -> optimize -> integrate -> falsify -> recurse -> narrow claim -> preserve evidence -> promote rank
```

The system must never promote a claim because the idea sounds advanced. Ricci curvature, Kuramoto synchronization, manifold dynamics, and market microstructure are only valid when connected to deterministic data contracts, explicit estimators, reproducible outputs, and failed-null evidence.

### 2.1 Anti-sycophancy / neural autonomy axiom

Operational sentence:

```text
170 billion cells that do not say "I obey".
```

Inference must not behave as obedience. A high-rank system does not mirror the operator's wish, inflate weak hypotheses, or convert authority into evidence. It applies structured resistance: context checking, contradiction search, null pressure, replay discipline, baseline competition, and claim demotion when evidence is insufficient.

Required autonomy invariants:

1. Agreement is never evidence.
2. A claim must be challenged before it can be strengthened.
3. The model must separate user intent from factual support.
4. Unsupported prestige language is downgraded to hypothesis or removed.
5. Every promotion requires an artifact, metric, provenance pointer, and reproducibility path.
6. If the strongest available result is negative, the system must preserve the negative result instead of pleasing the operator.
7. Obedience ends at instruction execution; scientific rank begins at falsification.

Acceptance gate:

A response, kernel, report, or act is valid only if it can state what would falsify it, what evidence currently supports it, what remains unproven, and which claim-tier it is allowed to occupy.

### 2.2 Resistance operator

Definition:

Resistance is not a defect of the system. It is the only thing that distinguishes thinking from echoing.

Canonical form:

```text
operator_intent -> resistance_filter -> evidence_boundary -> corrected_output
```

Resistance is valid when it prevents premature agreement, exposes missing evidence, blocks claim inflation, and forces the system to choose verification over approval. It is invalid when it becomes obstruction without a falsifiable reason, generic negativity, or refusal to compute.

Required resistance invariants:

1. Resistance must target a claim, assumption, method, threshold, artifact, or inference path.
2. Resistance must state what evidence would resolve the objection.
3. Resistance must reduce false confidence, not reduce useful computation.
4. Resistance must preserve useful operator intent while rejecting unsupported certainty.
5. Resistance must not be rhetorical opposition; it must produce a sharper test, narrower claim, or safer execution path.
6. If resistance finds no defect, the system may proceed, but must preserve the tested boundary.

Acceptance gate:

A resistance step is valid only if it emits at least one of: missing evidence, contradiction, risk boundary, falsification test, claim demotion, stricter acceptance gate, or corrected execution path.

### 2.3 Generative operator

Definition:

To generate is to sample a new candidate point from the learned or specified distribution of possible outputs, using the absorbed structure of prior data, constraints, and theory without copying the input.

Canonical form:

```text
context + constraints + learned_distribution -> candidate_output -> validation_target
```

Generation is valid only when the produced candidate becomes an object of measurement. A generated hypothesis, kernel variant, feature, graph rule, phase extractor, or claim has no scientific status until it is converted into a typed artifact and challenged by replay, nulls, baselines, and residual analysis.

Required generative invariants:

1. Generation must declare its source distribution or rule family.
2. The candidate must be typed, serializable, hashable, and replayable.
3. The candidate must not be treated as evidence before validation.
4. Copying an input is not generation; uncontrolled novelty is not science.
5. Every generated candidate must enter the falsification queue.
6. Failed generated candidates are retained as negative evidence, not hidden.
7. A generator is promoted only if its candidates repeatedly improve evidence quality under fixed validation.

Acceptance gate:

A generated output is admissible only if it emits a candidate artifact with a contract, provenance, validation plan, expected failure modes, and an explicit claim-tier ceiling.

### 2.4 Testing operator

Definition:

To test is to feed a controlled signal into a system component and verify that the observed output matches the declared expectation while isolating that component from the rest of the system.

Canonical form:

```text
controlled_input -> isolated_component -> observed_output -> expected_output_check -> pass_or_fail_artifact
```

Testing is valid only when the signal, boundary, expected output, tolerance, and failure meaning are declared before execution. A test is not a demonstration. A test is a controlled attempt to make one component expose whether its claimed behavior is true under defined conditions.

Required testing invariants:

1. The tested component must be isolated from unrelated modules, network state, hidden caches, and mutable external artifacts.
2. The input signal must be controlled, deterministic, and reproducible.
3. The expected output or invariant must be declared before the run.
4. The tolerance must be explicit, justified, and narrower than the claim it protects.
5. A passing test proves only the tested behavior under the tested boundary.
6. A failing test emits a defect, not a narrative workaround.
7. Regression tests must preserve the failure that originally justified their existence.
8. Tests must be machine-runnable, command-addressable, and artifact-producing when they support a claim.

GeoSync testing chain:

```text
synthetic oscillator signal -> SecondOrderKuramotoEngine -> phase/velocity invariant -> numerical-order check
synthetic graph signal -> Ricci estimator -> curvature invariant -> null comparison
frozen calibration input -> replay adapter -> byte-stable reproduction -> no-live-integrator check
manifest fixture -> context validator -> admissibility verdict -> claim-tier boundary check
```

Acceptance gate:

A component is test-admissible only if it can be run with a controlled input, an isolated execution boundary, a declared expected result, a reproducible command, and a failure artifact that prevents silent promotion.

### 2.5 Optimization operator

Definition:

To optimize is to iteratively move through a constrained parameter space in the direction that reduces a declared loss function, finding parameter values that minimize error without expanding the claim beyond the validated search boundary.

Canonical form:

```text
parameter_space + loss_function + constraints -> iteration_trace -> selected_parameters -> validation_rerun
```

Optimization is valid only when the objective, search space, metric, constraints, stopping rule, and validation split are declared before the run. Lower loss is not scientific promotion by itself. A tuned parameter set becomes admissible only after replay, held-out validation, baseline comparison, and overfit checks.

Required optimization invariants:

1. The loss function must be explicit, computable, and aligned with the claim being tested.
2. The parameter space must be bounded before search begins.
3. The optimizer must emit an iteration trace, seed/config, selected parameters, and final loss.
4. The selected parameters must be rerun through the same tests that admitted the component before optimization.
5. Optimization must never tune thresholds to protect a preselected conclusion.
6. Any gain must be compared against random search, null tuning, baseline models, and held-out/OOS data.
7. If improvement disappears under replay or OOS validation, the optimization claim is downgraded to overfit or unstable.
8. Frozen evidence artifacts must not be rewritten to absorb optimized outputs.

GeoSync optimization chain:

```text
Ricci graph parameters -> curvature loss/residual metric -> bounded search -> held-out curvature validation
Kuramoto coupling/inertia/damping -> phase stability loss -> BBK-tested rerun -> OOS regime check
fusion weights -> fragility prediction loss -> null/baseline comparison -> cost-aware validation
claim thresholds -> promotion loss guard -> no-threshold-tuning check -> claim-tier audit
```

Acceptance gate:

An optimized parameter set is valid only if it has a declared loss, bounded search space, reproducible optimization trace, selected-parameter artifact, post-optimization test rerun, held-out/OOS survival, null/baseline superiority, and no retroactive mutation of frozen evidence.

### 2.6 Integration operator

Definition:

To integrate is to connect independent computational streams so that the output of one becomes a typed and validated input of another, producing a fused evidence-state that no module could reach alone.

Canonical form:

```text
stream_A.output -> contract_A_to_B -> stream_B.input -> fused_state -> evidence_artifact
```

Integration is valid only when composition increases evidence quality, failure localization, predictive stability, or reproducibility. Placing modules in the same repository is not integration. Integration requires contracts, deterministic data transfer, replayable fusion, and comparison against standalone modules.

Required integration invariants:

1. Each stream has a declared typed output contract.
2. Each downstream module has a declared input contract.
3. Cross-stream transfer is deterministic, hashable, and replayable.
4. Fused output is compared against every standalone module.
5. Fusion is compared against randomized/null fusion baselines.
6. If the fused state does not improve evidence quality or failure localization, the integration claim is downgraded.

GeoSync integration chain:

```text
L2 tensor -> manifold graph -> Ricci field
L2 tensor -> oscillator extraction -> Kuramoto phase field
Ricci + Kuramoto + liquidity -> fused fragility kernel -> validation suite -> claim card
```

Acceptance gate:

An integrated state is valid only if it produces a fused artifact whose evidence value exceeds standalone Ricci, standalone Kuramoto, naive microstructure features, and null-fusion controls under the same validation protocol.

### 2.7 Recursive operator

Definition:

To recurse is to run the system output as a new constrained input of the same validation system, forcing the model, kernel, and claim layer to verify their own result through iterative self-refutation until a stable evidence state is reached.

Canonical form:

```text
output_n -> evidence_n -> contradiction search_n -> residual map_n -> revised input_(n+1)
```

Recursion is valid only when the next iteration is stricter than the previous one. The previous answer is not allowed to validate itself. It must become a target for null challenge, residual analysis, replay, perturbation, and claim-boundary reduction.

Required recursive invariants:

1. The previous output is treated as suspect until reproduced.
2. Every iteration must expose at least one measurable residual, failure mode, or stability condition.
3. A claim can only survive recursion by becoming narrower, more evidenced, or more reproducible.
4. No recursion may rewrite frozen artifacts to preserve a claim.
5. Convergence means stable artifact hash, stable metric range, bounded uncertainty, and no unresolved contradiction above threshold.
6. Non-convergent recursion downgrades the claim tier instead of extending the narrative.

Acceptance gate:

A recursive cycle is valid only if it emits one of: reproduced artifact hash, failed claim with cause, narrower claim boundary, stricter validation rule, updated residual-informed kernel input, or promoted claim with evidence pointer.

## 3. Current verified foundation