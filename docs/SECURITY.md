# Security Governance

- TACL Guard enforces monotonic free-energy descent and blocks unsafe parameter updates.
- JSONL logs capture every action with seven-year audit fields.
- Prometheus metrics expose governance counters for monitoring pipelines.
