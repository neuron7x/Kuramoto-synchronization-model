# GeoSync — Act of Work: Ricci-Kuramoto Manifold Kernel

Status: canonical planning / execution-control artifact  
Scope: `ricci_microstructure_v1` → `ricci_kuramoto_manifold_kernel_v1`  
Repository category: verification-first quantitative research infrastructure  
Claim boundary: hypothesis-tier until real-data evidence, null baselines, cost model, and reproduction gates pass

---

## 1. Purpose

This act formalizes the work required to move the current GeoSync Ricci research line from an instrumented hypothesis into a falsifiable, evidence-bearing, reviewer-grade research kernel.

The target is not to claim alpha, not to market a trading bot, and not to dress a chart as physics. The target is to build a deterministic Ricci-Kuramoto manifold kernel that can test whether market microstructure geometry and phase synchronization contain predictive information about regime breaks, volatility expansion, liquidity fragility, or systemic transition states.

Canonical framing:

```text
L2_ORDERBOOK_STREAM
  → microstructure tensors
  → dynamic multiplex graph
  → Ricci curvature field
  → Kuramoto phase field
  → curvature-modulated coupling
  → regime / fragility score
  → null-tested evidence artifact
```

---

## 2. Current Repository State

The repository already contains the correct epistemic spine:

1. Claim promotion is explicitly tiered.
2. Evidence-bearing artifacts require deterministic provenance.
3. Forbidden claim language is separated from admissible status language.
4. Placeholder artifacts are not allowed to support measured claims.
5. `ricci_microstructure_v1` is correctly marked as `HYPOTHESIS`, `INSTRUMENTED`, and `NOT EVIDENCE-BEARING`.

The existing Ricci implementation operates on cross-asset return-correlation graphs. This is useful as a structural adapter and deterministic topology test surface, but it is not yet an L2 microstructure Ricci manifold and must not be described as predictive evidence.

---

## 3. Defect / Gap Statement

Current blocker:

```text
B-001: No final Ricci-Kuramoto manifold kernel exists.
```

Impact:

```text
GeoSync cannot yet promote the Ricci line from HYPOTHESIS to MEASURED.
GeoSync cannot yet claim predictive power.
GeoSync cannot yet support external reviewer-grade scientific ranking for this mechanism.
```

Root cause:

```text
The repository has a strong verification framework and a Ricci correlation-graph adapter, but lacks real L2 market microstructure ingestion, deterministic manifold construction, Kuramoto phase extraction, null-superiority tests, and signed multi-session evidence artifacts.
```

---

## 4. Work Packages

### WP-1 — Data Contract: Real L2 Session Substrate

Objective: introduce immutable real market microstructure sessions.

Required outputs:

- `data/contracts/l2_session.schema.json`
- `configs/ricci_kuramoto/l2_session.yaml`
- `artifacts/data/l2_sessions/<session_id>/manifest.json`
- SHA-256 for raw input, normalized input, config, and environment

---

## 5. Inference-Control Operation: Extrapolate

Canonical operation:

```text
Extrapolate = extend the intent vector beyond the explicit wording while preserving the evidence boundary.
```

Operational definition:

```text
To extrapolate is to infer the next necessary computational, architectural, or validation state already implied by the structure of the signal, without inventing unsupported claims.
```

For this workstream, extrapolation means:

```text
Do not stop at "Ricci exists".
Infer the missing kernel path:
Ricci correlation adapter → L2 manifold substrate → Kuramoto phase field → fused fragility predictor → null-tested evidence artifact → claim-tier promotion gate.
```

Constraint:

```text
Extrapolation may expand implementation scope, validation scope, and artifact structure.
Extrapolation must not expand scientific claim status without evidence.
```

Failure mode:

```text
If extrapolation creates stronger claims than the artifacts support, it becomes hallucination.
If extrapolation creates stronger tests than the current artifacts support, it becomes engineering progress.
```

Acceptance rule:

```text
A valid extrapolated action must produce at least one of:
1. a stricter data contract;
2. a stronger null baseline;
3. a deterministic artifact;
4. a narrower claim boundary;
5. a clearer next implementation unit.
```

---

## 6. Inference-Control Operation: Recurse

Canonical operation:

```text
Recurse = feed the system output back into the same verification system as a new input until the claim either stabilizes or collapses.
```

Operational definition:

```text
To recurse is to force every generated kernel output, metric, score, claim, and artifact to become the next test object for the same pipeline, so that the system repeatedly attempts to disprove its own previous state.
```

For this workstream, recursion means:

```text
Ricci-Kuramoto output
  → evidence artifact
  → claim classifier
  → null/baseline challenge
  → replay validator
  → residual/error analysis
  → revised kernel input
  → narrower or stronger next claim
```

The recursion target is not rhetorical refinement. The recursion target is epistemic convergence.

Required recursive loop:

```text
Loop-0: build candidate kernel output.
Loop-1: test against deterministic replay.
Loop-2: test against shuffled-phase null.
Loop-3: test against shuffled-edge/null-graph baseline.
Loop-4: test against block-bootstrap session resampling.
Loop-5: test against naive volatility / VAR / GARCH / HMM baselines.
Loop-6: run walk-forward OOS split.
Loop-7: apply cost/slippage model.
Loop-8: classify admissible claim tier.
Loop-9: emit next stricter work package or stop.
```

Stop rule:

```text
STOP_STABLE if three consecutive recursive runs produce:
1. identical artifact hashes for deterministic replay;
2. no claim-tier inflation;
3. no failing null/baseline superiority gate;
4. no unexplained residual concentration;
5. no metric drift beyond configured tolerance.
```

Collapse rule:

```text
COLLAPSE if recursive validation shows that the signal disappears under nulls, costs, OOS splits, or replay constraints.
```

Promotion rule:

```text
A claim may move from HYPOTHESIS to MEASURED only if recursion produces stable evidence artifacts that survive replay, nulls, baselines, OOS, costs, and residual audit.
```

Failure mode:

```text
If recursion only rewrites language, it is narrative polishing.
If recursion makes the next run harder to fake, it is verification pressure.
If recursion destroys the claim, the system worked.
```

Acceptance rule:

```text
A valid recursive action must produce at least one of:
1. a reproduced artifact hash;
2. a failed claim with explicit cause;
3. a stricter validation gate;
4. a reduced claim boundary;
5. an updated kernel input generated from measured residuals.
```
