# N7X Value Synthesis Context

## Source status

This document is operator-provided synthesis context for PR #1204.

It is not repository evidence. External references must be independently retrieved before they support project claims.

## Compression

The synthesis is reduced into seven execution values:

1. Safety as architecture.
2. Signal over noise.
3. Judgment over brittle rules.
4. Understanding before action.
5. Jaggedness instrumentation.
6. Human oversight as control surface.
7. Honest helpfulness.

## Operational meaning for PR #1204

The inference transformer control plane must stay narrow, reversible, reviewable, and evidence-bound.

The repository must not treat role prompts, past sessions, or operator-provided essays as empirical proof.

The correct artifact is a machine-checkable control surface:

```text
schema -> config -> typed certificate -> placeholder artifact -> focused CI -> Claude handoff
```

## Product-quality threshold

A change is not complete because it sounds coherent.

A change is complete only when it has:

- typed boundary objects,
- deterministic checks,
- failure-path tests,
- clear blast-radius limits,
- source-status labeling,
- no claim-tier promotion without evidence.

## Code Claude rule

Read `.claude/value_functions/n7x_value_functions.v1.yaml` before implementation work in this PR.

Use the values as decision pressure, not as a substitute for tests.

⊛
