# SPDX-License-Identifier: MIT
# Lightweight requirements for security scanning (excludes torch and GPU dependencies)
# This file is used by the 'scan' stage in the Dockerfile to reduce image size.
# Security-critical floors MUST mirror requirements.txt / constraints/security.txt
# so scan-only resolution cannot drift below deploy-time patched versions.

# Core analytics stack (excluding torch)
numpy>=2.3.3
scipy>=1.16.2
pandas>=2.3.3
networkx>=3.5
deap>=1.4.1
scikit-learn>=1.3.0
joblib>=1.4.2
SQLAlchemy>=2.0.51
optuna>=3.6.1,<4.0.0
numba>=0.60.0

# Data validation
pydantic>=2.13.4,<3.0.0
pydantic-settings>=2.14.2,<3.0.0
pandera>=0.32.0,<1.0.0
exchange-calendars>=4.11.1

# Configuration & instrumentation
PyYAML>=6.0.3
hydra-core>=1.3.2
omegaconf>=2.3.0
prometheus-client>=0.25.0
opentelemetry-api>=1.38.0
opentelemetry-sdk>=1.38.0
opentelemetry-exporter-otlp>=1.38.0
aiokafka>=0.11.0
pyarrow>=24.0.0
redis>=7.0.0
httpx>=0.28.1
websockets>=15.0.1
ccxt>=4.5.12
tenacity>=9.1.2
aiolimiter>=1.2.1
fastapi>=0.139.0,<1.0.0
strawberry-graphql[fastapi]>=0.319.0
alembic>=1.14.0
psycopg[binary]>=3.2.3

# Optional feature store backends are available via `pip install geosync[feature_store]`

# Templates & packaging helpers
click>=8.3.0
jinja2>=3.1.6
uvicorn>=0.44.0

# Dashboard & visualization
streamlit>=1.54.0
streamlit-authenticator>=0.3.1

# Security: security-critical transitive dependencies are pinned via constraints/security.txt
# Direct floors below are raised to patched versions so scan-only resolution cannot
# select a vulnerable release. Mirrors requirements.txt / constraints/security.txt.
PyJWT[crypto]>=2.13.0
cryptography>=49.0.0
requests>=2.34.2

# Transitive HTTP/ASGI stack pinned to patched floors (enter via fastapi/streamlit/strawberry):
aiohttp>=3.14.1
starlette>=1.6.0
tornado>=6.5.7
python-multipart>=0.0.32
