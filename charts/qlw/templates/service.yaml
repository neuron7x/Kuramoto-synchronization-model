apiVersion: v1
kind: Service
metadata:
  name: {{ include "tradepulse-qlw.fullname" . }}
  labels:
    {{- include "tradepulse-qlw.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tradepulse-qlw.selectorLabels" . | nindent 4 }}
